server:
  port: 8072

eureka:
  instance:
    preferIpAddress: true  #the purpose of this is to share the ip adress of this instance to oter micro services
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
